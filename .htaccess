# Muslim Clock Web - Apache config
Options -Indexes
DirectoryIndex index.php

# Block sensitive file types
<FilesMatch "\.(env|sql|md|log)$">
    Require all denied
</FilesMatch>

# Block raw config file (still loaded by PHP via require)
<Files "config.php">
    Require all denied
</Files>
